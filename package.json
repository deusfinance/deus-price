{
  "name": "deus-price",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ vahid-dev/deus-price",
    "create-local": "graph create --node http://localhost:8020/ vahid-dev/deus-price",
    "remove-local": "graph remove --node http://localhost:8020/ vahid-dev/deus-price",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vahid-dev/deus-price"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
